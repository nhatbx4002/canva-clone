
export const initializeFarbic = async (canvasEl , containerEl) => {
    try{

    }catch(err){
        console.error("Canvas initialize Farbic Error" , err);
        return null;
    }
}

export const centerCanvas = (canvas) => {
    if(!canvas) return
}